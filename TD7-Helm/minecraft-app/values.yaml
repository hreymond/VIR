# Default values for our chart
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Deployment variables
replicaCount: 1
image:
  repository: docker.io
  namespace: hreymond
  name: virwebsite
  tag: v4

# Route variables
route:
  hostname: "minecraft.localhost"